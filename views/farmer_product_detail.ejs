<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details</title>
    <link rel="stylesheet" href="/stylesheets/global.css">


    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

</head>
<body class="bg-gray-100">
    <div class="container mx-auto py-8">
        <div class="max-w-4xl mx-auto bg-white shadow-md p-8">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <div class="md:col-span-1">
                    <img src="<%= product.productImage %>" alt="Product Image" class="w-full rounded-lg">
                </div>
                <div class="md:col-span-1">
                    <h2 class="text-2xl font-bold mb-4"><%= product.productName %></h2>
                    <p class="text-gray-600 mb-4"><%= product.description %></p>
                    <p class="text-gray-700 font-semibold mb-4">Features:</p>
                    <ul class="list-disc pl-6 mb-4">
                        <li><%= product.features %></li>
                    </ul>
                    <p class="text-gray-700 font-semibold mb-4">Price: $<%= product.price.toFixed(2) %></p>
                    <a href="/kisaan/payment/<%= product._id %>" class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded inline-block">Add to Cart</a>

            </div>
        </div>
    </div>
    
</body>
</html>
