<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FARM-WEBSITE</title>
    <link rel="stylesheet" href="/stylesheets/output.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet"/>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .login-form {
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 1rem;
            padding: 2rem;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>
<body class="bg-gradient-to-r from-red-400 via-purple-500 to-indigo-600 flex items-center justify-center">
    <div class="bg-blue-950 bg-opacity-50 shadow-xl max-w-[960px] grid grid-cols-2 items-center gap-20 p-5 rounded-2xl object-cover">
        <div class="ml-6">
            <img src="/images/officer.jpg" alt="" class="rounded-xl"/>
        </div>

        <div class="max-w-80 grid gap-5 text-white">
            <h1 class="text-5xl font-bold">Login</h1>

            <div class="flex items-center justify-center space-x-4 mt-4">
                <a href="#" class="flex items-center justify-center w-12 h-12 bg-gradient-to-r from-blue-400 to-cyan-200 font-semibold rounded-full">
                    <i class="ri-google-line text-blue-800 text-xl"></i> 
                </a>
                
                <a href="#" class="flex items-center justify-center w-12 h-12 bg-gradient-to-r from-blue-400 to-cyan-200 font-semibold rounded-full">
                    <i class="ri-facebook-box-fill text-blue-800 text-xl"></i>
                </a>
            </div>
            
            <form action="/officer/login" method="post" class="space-y-6 login-form">
                <div class="relative">
                    <div class="absolute top-1 left-1 bg-white-medium rounded-full p-2 flex items-center justify-center text-blue-300">
                        <i class="ri-user-line"></i>
                    </div>
                    <input type="text" name="username" placeholder="Username" class="w-full bg-white-light py-2 px-4 rounded-full focus:bg-black-dark focus:outline-none focus:ring-1 focus:ring-neon-blue focus:drop-shadow-lg" id="username-input"/>
                </div>
                <div class="relative">
                    <div class="absolute top-1 left-1 bg-white-medium rounded-full p-2 flex items-center justify-center text-blue-300">
                        <i class="ri-lock-line"></i> 
                    </div>
                    <input type="password" name="password" placeholder="Password" class="w-full bg-white-light py-2 px-4 rounded-full focus:bg-black-dark focus:outline-none focus:ring-1 focus:ring-neon-blue focus:drop-shadow-lg" id="password-input"/>
                </div>
                <button type="submit" class="bg-gradient-to-r from-blue-400 to-cyan-200 w-full font-semibold rounded-full py-2">Sign In</button>
            </form>
            
            <div class="text-dull-white border-t border-white-light pt-4 space-y-4 text-sm">
                <p>Don't have an account? <a href="#" class="text-blue-200 font-semibold cursor-pointer">Sign up</a></p>
                <p>Forgot password? <a href="#" class="text-blue-200 font-semibold cursor-pointer">Reset password</a></p>
                <p>Don't have a password yet? <a href="#" class="text-blue-200 font-semibold cursor-pointer">Set password</a></p>
            </div>

        </div>
    </div>

    <script>
        const usernameInput = document.getElementById('username-input');
        const passwordInput = document.getElementById('password-input');

        usernameInput.addEventListener('input', () => {
            usernameInput.style.color = 'black';
        });

        passwordInput.addEventListener('input', () => {
            passwordInput.style.color = 'black';
        });
    </script>
</body>
</html>


