<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Details</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .review-card {
            max-height: 400px;
            overflow-y: auto;
        }
    </style>
</head>
<body class="bg-gray-100">

    <nav class="bg-blue-500 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-white text-4xl font-bold">Blog Details</h1>
        </div>
    </nav>

    <div class="container mx-auto py-8">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <div class="col-span-2">
                <img src="<%= blog.image %>" alt="Blog Image" class="w-full h-80 object-cover rounded-md mb-4 shadow-lg">
                <div class="bg-white rounded-lg shadow-md p-6 mb-8">
                    <h3 class="text-2xl font-semibold mb-4"><%= blog.heading %></h3>
                    <p class="text-gray-700 mb-4"><%= blog.content %></p>
                    <p class="text-gray-500 mb-4">Written by <%= blog.officer %> on <%= blog.createdAt.toDateString() %></p>
                    <p class="text-gray-500">Designation: <%= blog.designation %></p>
                </div>

                <div class="bg-white rounded-lg shadow-md p-6 mb-8">
                    <h3 class="text-2xl font-semibold mb-4">Write a Review</h3>
                    <form action="#" method="POST">
                        <textarea name="comment" id="comment" rows="4" class="w-full px-4 py-2 border rounded-md mb-4" placeholder="Write your review..."></textarea>
                        <input type="file" name="image" id="image" class="mb-4">
                        <button type="submit" class="bg-blue-500 text-white px-6 py-2 rounded-md hover:bg-blue-600">Submit</button>
                    </form>
                </div>

                <div class="review-card">

                </div>
            </div>

            <div class="col-span-1">

            </div>
        </div>
    </div>

    <script>

    </script>

</body>
</html>
